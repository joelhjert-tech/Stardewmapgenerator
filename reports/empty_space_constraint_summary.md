# Empty Space Constraint Summary

- Generated: 2026-06-16T08:20:32+00:00

Empty tiles are now recorded as structural evidence. This matters most for walkable space in front of walls, doors, paths, ladders, and exits.

## Structural

- Pattern `516d779ced8c` requires `NW:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `N:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `NE:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `W:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `C:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `E:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `SW:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `S:Buildings` empty in 1782 observations.
- Pattern `516d779ced8c` requires `SE:Buildings` empty in 1782 observations.
- Pattern `c84529002172` requires `NW:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `N:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `NE:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `W:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `C:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `E:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `SW:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `S:Buildings` empty in 1687 observations.
- Pattern `c84529002172` requires `SE:Buildings` empty in 1687 observations.
- Pattern `04507f1580fa` requires `NW:Buildings` empty in 1448 observations.
- Pattern `04507f1580fa` requires `N:Buildings` empty in 1448 observations.
- Pattern `04507f1580fa` requires `NE:Buildings` empty in 1448 observations.
- Pattern `04507f1580fa` requires `W:Buildings` empty in 1448 observations.
- Pattern `04507f1580fa` requires `C:Buildings` empty in 1448 observations.
- Pattern `04507f1580fa` requires `E:Buildings` empty in 1448 observations.
- Pattern `04507f1580fa` requires `SW:Buildings` empty in 1448 observations.

## Decoration

- Pattern `19c2fe20d3a4` requires `NW:Back` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NW:Buildings` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NW:Front` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NW:Paths` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `N:Back` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `N:Buildings` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `N:Front` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `N:Paths` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NE:Back` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NE:Buildings` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NE:Front` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `NE:Paths` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `W:Back` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `W:Buildings` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `W:Front` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `W:Paths` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `C:Back` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `C:Buildings` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `C:Front` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `C:Paths` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `E:Back` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `E:Buildings` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `E:Front` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `E:Paths` empty in 2429 observations.
- Pattern `19c2fe20d3a4` requires `SW:Back` empty in 2429 observations.
